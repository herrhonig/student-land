---
pre-commit:
  parallel: false
  commands:
    prettier:
      run: npx prettier --write {staged_files}
      glob: "src/**/*.{js,jsx,ts,tsx,json,yml,yaml}"
    eslint:
      run: npx eslint {staged_files}
      glob: "src/**/*.{ts,tsx}"
    tsc:
      run: npx tsc -p tsconfig.app.json --noEmit